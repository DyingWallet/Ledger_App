<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/recordFrag">

    <fragment
        android:id="@+id/recordFrag"
        android:name="stu.xuronghao.ledger.fragment.RecordFrag"
        android:label="fragment_record"
        tools:layout="@layout/fragment_record" />
    <fragment
        android:id="@+id/reportFrag"
        android:name="stu.xuronghao.ledger.fragment.ReportFrag"
        android:label="fragment_report"
        tools:layout="@layout/fragment_report" />
    <fragment
        android:id="@+id/userFrag"
        android:name="stu.xuronghao.ledger.fragment.UserFrag"
        android:label="fragment_user"
        tools:layout="@layout/fragment_user" />
</navigation>